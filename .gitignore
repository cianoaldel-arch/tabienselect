# ==========================================================
# Root-level ignores (per-subproject ignores live in their own .gitignore)
# ==========================================================

# Environment files — each environment (dev machine, prod server) keeps its own.
# Commit .env.example instead.
.env
.env.*
!.env.example

# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Logs
*.log
